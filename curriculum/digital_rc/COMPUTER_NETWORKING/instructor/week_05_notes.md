# Packet Range — instructor week 5

**Hex/graph on the board:** seq=1000 len=200 → ACK 1200 on full receipt. cwnd 10, loss → 5, then 6,7,8... on additive increase per RTT without further loss.

**Lab `lab_datapath`:** student must submit parse/table JSON. Empty fails.

**Pitfall:** rwnd vs cwnd confusion: label receiver belly vs network belly.

No CS144 code, no CCNA dumps. Alignment JSON only.
