# Packet Range — instructor week 7

**Hex/graph on the board:** dijkstra(A)['D']=4 via B. Scenic A-C-D=10 loses. Install 10.20.40.0/24 nh=Yard.

**Lab `lab_spf_routing`:** student must submit parse/table JSON. Empty fails.

**Pitfall:** When you set B-D=20, wait until someone actually recomputes 10 vs 22.

No CS144 code, no CCNA dumps. Alignment JSON only.
