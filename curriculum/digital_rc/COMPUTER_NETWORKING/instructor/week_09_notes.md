# Packet Range — instructor week 9

**Hex/graph on the board:** decide(23)=deny, decide(443)=permit, decide(9)=deny. NAT still maps the inside host.

**Lab `lab_nat_acl`:** student must submit parse/table JSON. Empty fails.

**Pitfall:** If permit ip any any is line 1, the ACL is wallpaper — fail it.

No CS144 code, no CCNA dumps. Alignment JSON only.
