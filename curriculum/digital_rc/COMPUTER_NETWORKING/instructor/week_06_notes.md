# Packet Range — instructor week 6

**Hex/graph on the board:** Two access cables into one closet without STP → storm. BPDU guard on access would err-disable the volunteer mini-switch instead of electing it root.

**Lab `lab_vlan_mac`:** student must submit parse/table JSON. Empty fails.

**Pitfall:** The loop postmortem needs a blocked port, not 'STP magic'.

No CS144 code, no CCNA dumps. Alignment JSON only.
