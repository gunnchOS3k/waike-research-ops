# Assignment A04 — Forwarding plane — TTL, LPM, and a crafted IPv4 frame

On paper, slice the hex frame into MAC-dst, MAC-src, ethertype, IP version/IHL, TTL, proto, src, dst. Then run the lab and reconcile.

Lab to run: `lab_datapath`
