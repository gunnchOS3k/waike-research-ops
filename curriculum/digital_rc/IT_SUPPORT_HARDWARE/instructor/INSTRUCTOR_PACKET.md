# Instructor packet — IT Support and Hardware Foundations

Track: `IT_SUPPORT_HARDWARE`

## Keys / HITL
- Keys: use `GENERAL_IT/instructor/answer_keys.json` for shared labs; track-specific keys in `instructor/answer_keys.json` when present.
- Never copy keys into learner ingest.
- HITL: humans grade track assessments; AI never silently grades.
- HITL also applies to sudo elevation and after-hours change windows.

## Delivery
- Do not duplicate GENERAL_IT lesson files into this package.
- Emphasize track focus in discussions and assessments: Support + hardware foundations: power-first triage, storage/memory checks, ESD-safe handling, and change windows.
