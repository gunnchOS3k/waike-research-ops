# Assignment A03 — Cloud-shaped edges and the kiosk that must not walk into the SOC

Draw Harbor's zone matrix and write three sentences on shared responsibility if tickets moved to a SaaS. No vendor brochure language.

Lab to run: `lab_segmentation_zones`
