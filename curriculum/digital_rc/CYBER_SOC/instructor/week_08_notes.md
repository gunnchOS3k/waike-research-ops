# Harbor SOC — instructor week 8

**Tabletop beat:** unsafe(\x14short) returns a short slice (the lie). safe(\x14short) raises ValueError. safe(\x04abcd)==b'abcd'.

**Lab `lab_safe_vuln_detect`:** fixture + student JSON. Empty note fails no_attacker_word.

**Pitfall:** No sockets, no random IPs, no shellcode. Fixture only.

No ISC2/CompTIA item text. Upcoming CC 2026-09-01 is a domain map, not a dump.
