# Civic Tech Desk — instructor week 2

**Live number:** work A 4.7 GiB `.mkv` copied to a FAT32 camera card ends as a 0-byte or missing file. Reformat is the wrong first step. Copy to the NTFS staff volume, then to the patron exFAT share.

**Lab `lab_os_users`:** collect student JSON; do not run the golden path and call it theirs.

**Pitfall:** Do not let anyone format the camera card as the first move on a 4.7 GiB vanish.

Refuse vendor item banks. Point at `curriculum/alignment/general_it_alignment.json`.
