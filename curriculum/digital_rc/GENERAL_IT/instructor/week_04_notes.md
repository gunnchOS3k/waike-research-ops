# Civic Tech Desk — instructor week 4

**Live number:** work size=256GiB used=180 reserved=12 → free=64 GiB → 64/256=0.25 ≥ 0.15. SHA256 of source tree must equal SHA256 of restored tree.

**Lab `lab_backup`:** collect student JSON; do not run the golden path and call it theirs.

**Pitfall:** If they skip the restore hash, the .tgz is theater.

Refuse vendor item banks. Point at `curriculum/alignment/general_it_alignment.json`.
