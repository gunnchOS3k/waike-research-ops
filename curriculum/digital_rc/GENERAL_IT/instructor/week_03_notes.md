# Civic Tech Desk — instructor week 3

**Live number:** work kiosk uid 1010, groups=[kiosk], sudo=false. desk.lead uid 1020, groups=[helpdesk, staff]. If kiosk also sits in sudo, the lab fails even if the password is long.

**Lab `lab_os_users`:** collect student JSON; do not run the golden path and call it theirs.

**Pitfall:** Watch for copied /etc/passwd lines that keep UID 1020.

Refuse vendor item banks. Point at `curriculum/alignment/general_it_alignment.json`.
