# Assignment A03 — Users, groups, and the sudo you can actually type

Draw the account table for kiosk, desk.lead, and a volunteer `sat.am`. Mark sudo, groups, and one action each account must never do.

Lab to run: `lab_os_users`
