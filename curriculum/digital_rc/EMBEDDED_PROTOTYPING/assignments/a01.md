# Assignment A01 — MCU memory map — flash vs SRAM before first boot

Submit lab_ep_memory_map JSON for EP-4101. Map the nRF52840-class memory regions for ForgeSense subsystem firmware.

Lab to run: `lab_ep_memory_map`
