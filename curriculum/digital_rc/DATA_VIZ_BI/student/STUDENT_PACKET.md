# Student packet — DATA_VIZ_BI

Run Civic Metrics Studio for ten weeks: clean waits → SQL → stats → charts → KPI story → hashed portfolio dashboard.

You submit lab JSON you computed. You will not receive answer keys. Empty submissions fail. A file containing only PASS is rejected.
