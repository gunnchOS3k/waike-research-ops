# Assignment A06 — Guest protocol — ping/boot_status contract

Submit lab_gpl_guest_protocol JSON for GPL-5606. Guest agent protocol fields; obsolete click-collector rejected.

Lab to run: `lab_gpl_guest_protocol`
