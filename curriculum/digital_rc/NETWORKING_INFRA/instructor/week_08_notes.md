# Packet Range — instructor week 8

**Hex/graph on the board:** First resolve desk.gary.waike.example → 203.0.113.14 via auth_walk. Second → cache. NAT inside 10.20.30.14 outside 192.0.2.88.

**Lab `lab_dns_resolution`:** student must submit parse/table JSON. Empty fails.

**Pitfall:** Ask which liar expired: DHCP, DNS TTL, or NAT pool.

No CS144 code, no CCNA dumps. Alignment JSON only.
